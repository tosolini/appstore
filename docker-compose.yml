services:
  appstore-api:
    build:
      context: .
      dockerfile: Dockerfile
    image: container-appstore:latest
    container_name: container-appstore-api
    ports:
      - "8888:8888"
    environment:
      HOST: 0.0.0.0
      PORT: 8888
      CACHE_DIR: /app/cache
      GIT_SYNC_INTERVAL: 604800 # 7 days in seconds
      REPOSITORIES: |
        [
          {
            "name": "CasaOS AppStore",
            "url": "https://github.com/IceWhaleTech/CasaOS-AppStore.git",
            "branch": "main",
            "priority": 100,
            "enabled": true
          },
          {
            "name": "LinuxServer",
            "url": "https://github.com/WisdomSky/CasaOS-LinuxServer-AppStore.git",
            "branch": "main",
            "priority": 98,
            "enabled": false
          },
          {
            "name": "BigBear",
            "url": "https://github.com/bigbeartechworld/big-bear-casaos.git",
            "branch": "master",
            "priority": 96,
            "enabled": false
          },
          {
            "name": "Coolstore",
            "url": "https://github.com/WisdomSky/CasaOS-Coolstore.git",
            "branch": "main",
            "priority": 95,
            "enabled": false
          }
        ]
      # Default GitHub import seed (bundled github-imports.txt).
      # Reconciled on every startup: new URLs in the list are auto-imported
      # after commit + redeploy. Set GITHUB_IMPORT_SEED_URL to host the list
      # in a separate repo instead (then a restart is enough, no rebuild).
      GITHUB_IMPORT_SEED_FILE: "${GITHUB_IMPORT_SEED_FILE:-/app/github-imports.txt}"
      GITHUB_IMPORT_SEED_URL: "${GITHUB_IMPORT_SEED_URL:-}"
      # Optional: periodically re-check the list (seconds, 0 = startup only)
      GITHUB_IMPORT_SEED_SYNC_INTERVAL: "${GITHUB_IMPORT_SEED_SYNC_INTERVAL:-0}"
      # Optional: delete DB records whose URL was removed from the list
      GITHUB_IMPORT_SEED_PRUNE: "${GITHUB_IMPORT_SEED_PRUNE:-false}"

      # Active backend selection: "portainer", "arcane", or "auto" (default)
      ACTIVE_BACKEND: "${ACTIVE_BACKEND:-auto}"

      # Portainer integration - use host.docker.internal on macOS/Windows
      PORTAINER_BASE_URL: "${PORTAINER_BASE_URL:-http://host.docker.internal:9443}"
      PORTAINER_API_KEY: "${PORTAINER_API_KEY}"
      PORTAINER_ENDPOINT_ID: "${PORTAINER_ENDPOINT_ID:-3}"
      PORTAINER_VERIFY_SSL: "${PORTAINER_VERIFY_SSL}"
      MOCK_MODE: "${MOCK_MODE}"
      DEBUG_MODE: "${DEBUG_MODE}"

      # Arcane integration
      ARCANE_BASE_URL: "${ARCANE_BASE_URL}"
      ARCANE_API_KEY: "${ARCANE_API_KEY}"
      ARCANE_ENVIRONMENT_ID: "${ARCANE_ENVIRONMENT_ID:-0}"
      ARCANE_VERIFY_SSL: "${ARCANE_VERIFY_SSL:-false}"
      ARCANE_MODE: "${ARCANE_MODE:-auto}"

    volumes:
      - ./data:/app/data
      - appstore_cache:/app/cache
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro

    restart: unless-stopped

    networks:
      - appstore-net

volumes:
  appstore_cache:
    driver: local

networks:
  appstore-net:
    driver: bridge
